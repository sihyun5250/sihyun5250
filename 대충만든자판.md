```js
function solution(keymap, targets) {
    var answer = [];
   
    //1. targets 문자열 배열
    for(let targetArr of targets){
       
        //2. targetArr의 각 index로 접근
        for(let i=0; i<targetArr.length){
           
            //3. keymap에 들어있는 각각의 문자열 배열에서 targetArr의 index가 있는가?
  
            for()
           
        }        
    }
       
    return answer;
}
```
